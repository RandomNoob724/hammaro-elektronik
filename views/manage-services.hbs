<div class="container-fluid">
    <h1>Hantera Tjänster</h1>
    <table class="table table-striped col-md" id="project-table">
        <thead>
            <tr>
            <th scope="col">Titel</th>
            <th scope="col">Uppdatera</th>
            <th scope="col">Ta bort</th>
            </tr>
        </thead>
        <tbody>
            {{#if serviceList.length}}
                {{#each serviceList}}
                    <tr>
                        <td>{{title}}</td>
                        <td><a href="/service/update/{{id}}"><button class="btn btn-secondary">Uppdatera</button></a></td>
                        <td>
                            <form method="POST" action="/service/{{id}}/delete">
                                <input type="hidden" name="_csrf" value="{{../csrfToken}}">
                                <button class="btn btn-danger" type="submit">Ta bort</button>
                            </form>
                        </td>
                    </tr>
                {{/each}}
            {{else}}
                <td>No post found</td>
            {{/if}}
        </tbody>
    </table>
</div>